package Qublog::Server::View::TD;
use strict;
use warnings;
use base 'Catalyst::View::Template::Declare';
1;

# Put templates in separate modules, each under the 
# Qublog::Server::View::TD::* namespace.  Be sure to "use Template::Declare::Tags"
# in each one.

